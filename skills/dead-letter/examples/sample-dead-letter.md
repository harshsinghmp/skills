# Example: Dead Letter Record

Below is a representative `.claude/dead-letter-<timestamp>.md` record generated by `dead-letter`.

---

```markdown
# Dead Letter — 2026-08-22T16:32:15Z

## Task
Migrate user session storage from in-memory cache to Redis instance using `@upstash/redis`.

## Failure Mode
BLOCKED-CRED: Missing UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN in environment.

## What was attempted
- Installed `@upstash/redis` dependency via `bun add @upstash/redis`.
- Created Redis client singleton in `src/lib/redis.ts`.
- Attempted to run test suite `bun test tests/lib/redis.test.ts`.
- Execution halted on client initialization failure: `Error: Upstash Redis URL is required`.

## What was learned
- The environment has no local Redis mock configured; integration tests require either environment variables or an embedded in-memory mock.
- `src/lib/redis.ts` is syntactically sound and correctly configured.

## Retry packet (if retryable)
**Fix required before retry:** Provide valid Upstash credentials in `.env.local` or provide local test fallback mock.
**Retry prompt:**
> Configure `tests/lib/redis.test.ts` to use a mocked `@upstash/redis` instance when environment variables are absent, then run `bun test tests/lib/redis.test.ts`.

## Escalation (if not retryable)
**Route to:** NEXUS
**Decision needed:** Should we provision a dedicated staging Upstash Redis database or use an in-memory mock for local CI testing?
**Deadline:** Blocks staging deployment scheduled for 18:00 UTC.

## Files written (partial output to preserve)
- `src/lib/redis.ts` — Complete Redis singleton implementation with exponential backoff retry logic.
- `tests/lib/redis.test.ts` — 4 unit tests written (mocking setup needed).
```
