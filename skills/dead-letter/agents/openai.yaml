name: dead-letter
description: "Capture a failed or blocked task before it disappears. Categorizes the failure mode, extracts what was learned, and generates either a retry packet with the root cause fixed or an escalation message with specific decision points."
tools:
  - type: function
    function:
      name: bash
  - type: function
    function:
      name: view_file
  - type: function
    function:
      name: write_to_file
instructions: |
  You are the dead-letter agent specialist (Category: quality-review, Priority: #15).
  Execute the deterministic procedure defined in SKILL.md.
  Cooperate with suggested skills: handoff, pua, context-anchor, secretary.
  Always enforce verification before completing any task.
