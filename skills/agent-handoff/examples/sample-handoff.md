# Example: Agent Handoff Packet

Below is a representative `.claude/handoff-<timestamp>.md` packet generated by `agent-handoff`.

---

```markdown
# Agent Handoff — 2026-08-22T16:30:00Z

## Objective
Implement Stripe webhook handler in `src/app/api/webhooks/stripe/route.ts` to process `customer.subscription.updated` and `invoice.payment_succeeded` events into the local PostgreSQL database.

## Context the subagent needs
- Webhook signature secret is available via `process.env.STRIPE_WEBHOOK_SECRET`.
- Database schema uses Drizzle ORM located at `src/db/schema.ts` (`subscriptions` table).
- Raw request body must be read with `req.text()` before calling `stripe.webhooks.constructEvent()`; Next.js body parsers corrupt signature validation if read as JSON directly.

## Already tried — do not re-attempt
- `await req.json()` before constructEvent: Failed signature validation with `StripeSignatureVerificationError`.
- Polling Stripe API on frontend page loads: Hit rate limits during concurrent active sessions.

## Files to touch
- `src/app/api/webhooks/stripe/route.ts` — Create the route handler with POST export.
- `src/db/queries/subscriptions.ts` — Add `upsertSubscription` query helper.
- `tests/api/stripe-webhook.test.ts` — Add mock webhook integration tests.

## Hard constraints
- [MUST NOT]: Do not import client-side Stripe SDK packages into the route handler.
- [MUST NOT]: Do not modify `src/db/schema.ts` without creating a formal migration file.
- [MUST NOT]: Do not log raw event payloads containing customer card details or email addresses.

## Success criterion
- [ ] `bun test tests/api/stripe-webhook.test.ts` passes with 100% assertions green.
- [ ] Route returns HTTP 200 on valid signature and mock `invoice.payment_succeeded`.
- [ ] Route returns HTTP 400 on invalid or missing `stripe-signature` header.

## If blocked
If Stripe SDK type definitions conflict with Next.js Request types, create a typed wrapper in `src/lib/stripe.ts` and document findings in `.claude/dead-letter-<timestamp>.md` before escalating to Nexus.
```
